<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E3F2FD"
    android:padding="24dp"
    tools:context=".SeleccionProductoActivity">

    <AutoCompleteTextView
        android:id="@+id/autoProducto"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:hint="Buscar producto"
        android:background="@drawable/edittext_bg"
        android:popupBackground="@drawable/custom_dropdown_background"
        android:padding="12dp"
        android:textColor="#000000"
        android:textColorHint="#757575"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp" />

    <AutoCompleteTextView
        android:id="@+id/autoPresentacion"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:hint="Buscar presentación"
        android:background="@drawable/edittext_bg"
        android:popupBackground="@drawable/custom_dropdown_background"
        android:padding="12dp"
        android:textColor="#000000"
        android:textColorHint="#757575"
        app:layout_constraintTop_toBottomOf="@id/autoProducto"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />

    <Button
        android:id="@+id/btnContinuar"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:text="Continuar"
        android:textColor="#FFFFFF"
        android:background="@drawable/button_selector"
        app:layout_constraintTop_toBottomOf="@id/autoPresentacion"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
